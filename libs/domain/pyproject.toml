[project]
name = "domain"
version = "0.1.0"
dependencies = ["sqlalchemy>=2.0", "psycopg2-binary", "pydantic>=2.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/domain"]
